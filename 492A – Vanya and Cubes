#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;

    int height = 0;
    int total = 0;

    for (int i = 1; ; i++) {
        total += i * (i + 1) / 2;
        if (total > n) break;
        height++;
    }

    cout << height;
    return 0;
}
