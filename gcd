#include <bits/stdc++.h>
using namespace std;

int gcd(int a, int b) {
    while(b != 0) {
        int r = a % b; // remainder
        a = b;
        b = r;
    }
    return a;
}

int main() {
    int A, B;
    cin >> A >> B;

    cout << gcd(A, B);
    return 0;
}
