#include <iostream>
using namespace std;

int main() {
    int n, k;
    cin >> n >> k;

    int countEligible = 0;
    for (int i = 0; i < n; i++) {
        int skill;
        cin >> skill;
        if (skill + k <= 5)
countEligible++;
    }

    cout << countEligible / 3;
    return 0;
}
